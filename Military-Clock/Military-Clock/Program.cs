/* Requirements
 
    Task - Create a military style clock using C#.

    Requirements - The clock must be a 24 hour clock. Format:{ 23:59:59 }
                 - The clock must tick at 1 second per second.
                 - The clock is not required to display the current time.
                 
    Forbidden    - You may not use DateTime.Now or any DateTime methods.

    Notes        - If you write it twice, it should be a method.
                 - If you can make it dynamic, you might as well.
 */

namespace Military_Clock
{
    class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
